# 🇨🇳 중국 출장 체크리스트 — PIE 작업분석

> 2026-07-26 전 항목 실측 검증 완료. **인터넷·구글 없이 100% 동작합니다.**
> 상세 검증 근거는 `WORKLOG.md`의 "중국 출장 대비 오프라인 실전 점검" 표 참조.

---

## 1. 출발 전 (한국에서 반드시)

- [ ] **USB에 폴더 통째로 복사** — 이 폴더(`작업분석 중국`) 전체를 그대로 복사
  - ⚠️ 파일 몇 개만 골라 복사하면 **AI 기능이 작동하지 않습니다** (`mediapipe` 폴더 필수)
  - 용량 약 20MB
- [ ] **USB 2개 준비** (하나 고장 대비)
- [ ] 노트북에서 **미리 한 번 실행해 보기** — `PIE(중국).bat` 더블클릭 → 브라우저 열림 확인
- [ ] **라이선스 키 준비** — 현장 PC마다 첫 실행 때 입력 필요. 키를 메모/사진으로 보관
- [ ] 현장 PC에 **Chrome 또는 Edge**가 있는지 미리 확인 (저장·불러오기 기능에 필요)
  - 없을 것 같으면 Chrome 오프라인 설치본도 USB에 함께 담기

> 💡 **GitHub에서 받지 마세요.** 현지에서는 느리거나 막힐 수 있습니다. USB 반입이 확실합니다.

---

## 2. 현장 설치 (PC 1대당 5분)

**방법 A — 폴더 그대로 쓰기 (가장 간단, 추천)**
1. USB의 폴더를 PC 바탕화면이나 `D:\`에 통째로 복사
2. `PIE(중국).bat` 더블클릭
3. 검은 창이 뜨고 브라우저가 자동으로 열림 → 라이선스 키 입력
4. ⚠️ **검은 창은 끄지 마세요** (끄면 프로그램도 종료됩니다)

**방법 B — 설치 프로그램 사용**
1. `PIE_설치.bat` 더블클릭 → 설치 창에서 [설치] 클릭
2. 바탕화면에 **PIE 작업분석** 아이콘 생성 → 더블클릭으로 실행
3. 제거는 `PIE_제거.bat` 또는 Windows 설정 → 앱

> Windows 보안 경고(파란 화면)가 뜨면 **추가 정보 → 실행**을 누르세요. 바이러스가 아닌 로컬 스크립트입니다.

---

## 3. 현장 도착 후 즉시 확인 (5가지)

- [ ] **1) 실행되는가** — 브라우저에 PIE 화면이 뜨는가
- [ ] **2) 중국어로 바뀌는가** — 우측 상단 🇨🇳 中文 클릭 → 메뉴가 중국어로
- [ ] **3) 영상이 열리는가** — 작업 영상을 화면에 끌어다 놓기
- [ ] **4) 마킹이 되는가** — 영상 클릭(시작) → 다시 클릭(끝)으로 구간 기록
- [ ] **5) AI가 되는가** — 좌측 `AI 비전 분석` 실행 → 로딩 후 화면에 사람 뼈대가 그려지면 정상
  - ⚠️ AI가 안 되면: `PIE.html`을 직접 더블클릭한 것일 수 있음 → **반드시 `PIE(중국).bat`으로 실행**

---

## 4. 여러 PC에서 데이터 공유 (선택)

부품 표준시간(ST)을 여러 PC가 함께 쌓고 싶을 때만 필요합니다.

**방법 1 — 공유 폴더 (간단)**
- 각 PC에서 설정 > ST 누적 저장소 > **📁 폴더** 선택 → 같은 네트워크 공유 폴더 지정

**방법 2 — 서버 (안정적, 추천)**
1. PC 한 대에서 `PIE_ST_server_시작.bat` 실행
   - 처음 한 번 **관리자 권한 확인창**이 뜨면 [예]
   - 창에 표시되는 주소(예: `http://192.168.0.10:8792`)를 메모
2. 다른 PC들: 설정 > ST 누적 저장소 > **🌐 서버** → 위 주소 입력 → [지금 동기화]
3. ⚠️ 방화벽 차단 시 8792 포트 허용 필요

> 검증 완료: 표본이 많은 데이터가 자동 채택되고, 한 PC에서 지우면 다른 PC에서도 지워집니다.

---

## 5. 문제 생기면

| 증상 | 조치 |
|---|---|
| 브라우저가 안 열림 | 주소창에 `http://127.0.0.1:8791/PIE.html` 직접 입력 |
| AI(사람 뼈대)가 안 나옴 | `PIE.html` 직접 실행하지 말고 **`PIE(중국).bat`**으로 재실행 |
| 저장/불러오기 버튼이 안 먹음 | Chrome 또는 Edge에서 실행 (다른 브라우저 미지원) |
| 검은 창이 바로 닫힘 | 폴더를 통째로 복사했는지 확인 (`PIE_local_server.ps1` 있어야 함) |
| 라이선스 만료 메시지 | lovekhl83@gmail.com 문의 |
| 다른 PC와 동기화 안 됨 | 서버 PC의 검은 창이 켜져 있는지 + 방화벽 8792 허용 확인 |

---

## 6. 알아두기

- **인터넷 연결이 전혀 필요 없습니다.** 구글·외부 서버로 나가는 통신이 **0건**임을 실측 확인했습니다.
- 이 버전에는 **구글 시트 연동이 없습니다.** 모든 데이터는 현장 PC와 사내 공유 폴더에만 저장됩니다.
- 사용 설명서는 `PIE_가이드.html` (한국어·중국어·베트남어) — 중국 직원에게 中文 탭을 보여주세요.
- 메뉴의 "AI 동작분석"은 이 배포본에서 숨겨져 있습니다(별도 서버 필요). **"AI 비전 분석"은 정상 사용 가능**합니다.
